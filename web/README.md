# Web frontend

This is a plain HTML/CSS/JavaScript frontend.

Recommended local server:

```bash
npx serve .
```

Do not open HTML files directly with `file://` if the browser blocks API requests. A local HTTP server is recommended.
